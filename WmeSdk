/* eslint-disable no-unused-vars */

// Just a quick experiment to see if this helps with VSCode intellisense
class WmeSdk {
    DataModel = {
        Venues: {
            getById: args => null
        }
    };
}
